name: <%= File.basename(Dir.pwd) %> 
root: .

windows:
  - editor: nvim
  - shell:
      layout: even-horizontal
      panes:
        - cd frontend
        - cd backend
  - server:
      layout: even-horizontal
      panes:
        - cd frontend && bun run dev
        - cd backend && bun run dev
  - git: lazygit

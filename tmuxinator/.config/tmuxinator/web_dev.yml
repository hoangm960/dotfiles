name: <%= File.basename(Dir.pwd) %> 
root: .

windows:
  - editor: nvim
  - server:
      layout: even-horizontal
      panes:
        - cd frontend && yarn run dev
        - cd backend && yarn run dev
